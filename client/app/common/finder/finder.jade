.finder(ng-controller="finderController")
  input.form-control(finder-input type="text" placeholder="Search")
  .suggestions(ng-show="found.length > 0")
    div.suggestion(ng-repeat="f in found")
      .filename {{f.name}}
      .path {{f.path}} 


