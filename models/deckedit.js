function DeckEdit () {}

var singleton = new DeckEdit();

/**
 * getFlashcardsWithFilePath
 *
 * @param {number} userId
 * @param {string} path
 * @return {Array.<Object>} The list of flashcards which belong to the file
 *    at path
 */
DeckEdit.prototype.getFlashcardsWithFilePath = function (userId, path) {

};

module.exports = singleton;

